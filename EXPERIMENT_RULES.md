# 几条小约定

> 不是规矩，是为了下次踩到同样的坑能少走弯路。每条都有具体原因，不是凭空加的。

1. **改完 commit 一下**
   message 写啥都行（"调了 SetCrop"、"又试了一版" 都可以）。当天有就好。
   *原因：板子状态可回滚。两周前能跑的版本被改坏了，没 commit 就再也回不去。*

2. **AI 聊完，对话存到 `ai-chats/YYYY-MM-DD-话题.md`，再 commit**
   复制粘贴整段对话即可，格式不重要。
   *原因：你已经丢过几次串口报错排查的诊断过程，下次类似 bug 还得重头查。*

3. **模型改动前后，跑一次 `scripts/verify_model.py`**
   验证 ONNX vs 板上 a1model 输出是否一致。差距大就是模型转换坏了，再调上层代码徒劳。
   *原因：当前 §2 卡点很可能就是这个，不验证一直在错的基础上改。*

4. **遇到新问题就往 `DEVLOG.md` 里加一条**
   格式宽松，参照局部晴朗交接稿的写法即可。**失败的尝试也写**，不然下次又走一遍。

5. **每周日晚发一份 `reports/周报-YYYY-MM-DD.md`**
   就 3 个问题：这周搞定啥 / 现在卡哪 / 下周做啥。各一两句话。
   *原因：让项目负责人不用追着问就能掌握进度，你也不用每天报备。*
