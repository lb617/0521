# web_demo

FastAPI + 静态 HTML 的实时演示服务。

推荐从项目根目录启动：

```bash
bash scripts/run_web_demo.sh
```

或指定视频：

```bash
bash scripts/run_web_demo.sh /path/to/video.mp4
```

浏览器打开：

```text
http://127.0.0.1:7860
```
