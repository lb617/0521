# aimodel_lite_training

训练、评估、视频推理和 ONNX 导出脚本目录。

常用入口见项目根目录：

- `docs/02_RUN_GUIDE.md`
- `scripts/`

当前 `.pt` 权重已经整理到：

```text
artifacts/checkpoints/
```

当前 `.onnx` 模型已经整理到：

```text
artifacts/onnx/
```
